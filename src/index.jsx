import React from 'react';
import { render } from 'react-dom';

// eslint-disable-next-line no-undef
const rootElement = document.querySelector('#root');

render(<div>Negotiation App</div>, rootElement);
